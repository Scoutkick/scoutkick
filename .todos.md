# Secret TODO List

Things that need fixing but aren't urgent enough to block shipping.

## Packaging & Dev Workflow
- [ ] Kill the `sys.path.insert` hack in `scout.py` — make the backend `pip install -e .`-able with a proper entry point
- [ ] Make `backend/` an installable package with its own `pyproject.toml`, so imports are `from scoutkick.api...` instead of `from backend.src.api...`
- [ ] Publish `scoutkick-python` to PyPI so install is `pip install scoutkick-api` instead of the fragile `git+https` subdirectory path

## DevOps & CI
- [ ] Add GitHub Actions — at minimum run `pytest backend/tests/` on push
- [ ] Add a linter config (ruff or black) and type checking (pyright/pytype)
- [ ] Add `.env` support / pydantic-settings for configurable DB_PATH, port, etc.
- [ ] Use Render preview environments or a staging branch — auto-deploying `master` directly to prod is risky
- [ ] Pin Python version in `render.yaml` or add a `runtime.txt`

## Code Quality
- [ ] Split `cleaner.py` — it's 400+ lines mixing fragment definitions, aggregation logic, and field-to-vector mapping. One file per season or at least split fragments into their own module.
- [ ] Keep a DB schema version in the database and add migration logic so old `.db` files don't silently corrupt when the vector format changes
- [ ] Either build the `frontend/` directory or delete it — an empty planned directory is just noise

## Structure
- [ ] Consider flattening `backend/src/` — four levels of nesting (`backend/src/api/data.py`) is unnecessary when there's only one package. `backend/api/data.py` would read fine.
- [ ] `storage/__init__.py` and `api/deps.py` both resolve DB_PATH — consolidate to one canonical source (moved to `__init__.py` already, but `deps.py` still has a redundant import path)
